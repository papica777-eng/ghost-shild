/**
 * VERITAS CONFIG — PRODUCTION
 * O(1) constant lookup. No simulation.
 */
const API_BASE_URL = 'https://ghost-shild-122.onrender.com';

console.log('[QANTUM] CONFIG LOADED — API:', API_BASE_URL);
